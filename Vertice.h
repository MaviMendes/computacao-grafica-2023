#ifndef VERTICE_H
#define VERTICE_H

class Vertice {
    public:
        float x;
        float y;
        float z;
        Vertice();
        Vertice(float x, float y);
        Vertice(float x, float y, float z);
};

#endif